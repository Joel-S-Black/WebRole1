using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using System.Data;

namespace DatabaseObjectClass
{
    public static class DropDownListData
    {
       /* the code in this class will create list objects using ADO.NET. The resulting 
        * global variables will be stored in the viewstate or generated each new page, 
        * whichever is lighter weight. The lists will be used to provide drop down list options.
        * The appropriate controls in each page will bind to their global list variable names,
        * for example, "ddlTitle" will always bind to "lstTitle", etc. ADO.NET will return SQL Views
        * 
        */ 
        
    }
}